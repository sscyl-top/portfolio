# 2026-06-22

## Plan 2: Works & Media — P0 完成

### 新增 server actions
- `updateMediaBlock` — 编辑媒体块，复用 `mediaBlockSchema`（新增 `block_id` optional 字段），更新 `work_blocks` payload（media_id + caption），revalidate 对应路径
- `updateGalleryBlock` — 编辑图库块，新增 `galleryBlockUpdateSchema`，重新 enrichment media_refs 后更新 payload

### UI 改造
- `MediaBlockCard` → 从只读删除表单改为可编辑表单，含媒体选择 dropdown、caption 编辑、sort_order、is_visible 切换、"保存块"按钮，删除按钮通过 `formAction={deleteWorkBlock}` 保留
- `GalleryBlockCard` → 同上改为可编辑表单，媒体多选 checkbox 区预选已有素材，"保存块"持久化更新
- BlockEditor 描述文案更新："当前版本支持文本块、媒体块与图库块编辑"

### 修改文件
- `src/app/admin/(protected)/works/actions.ts` — 新增 2 个 action + 1 个 schema
- `src/app/admin/(protected)/works/[id]/page.tsx` — 重写 2 个块组件 + 新增 import

### 验证
- `npx tsc --noEmit` — 仅预存测试 mock 报错（非本次引入）
- `npm run build` — 通过，29 pages generated

---

## Admin 密码重置
- Supabase Auth 用户 `3624457672@qq.com` 已存在，admin role 已设置
- 密码重置为用户指定的 `Ak47522423.`

---

## Plan 2: Works & Media — P1 完成

### Palette 颜色编辑器
- `workUpdateSchema` 新增 `palette: z.array(paletteColorSchema).default([])`，每个颜色校验 hex 格式 `#RGB` 或 `#RRGGBB`
- `updateWork` action 解析逗号分隔的 palette 字符串为数组
- `PaletteEditor` 组件：显示现有色板 swatches + 逗号分隔 hex 输入框

### Slug 自动生成
- 新建 `SlugInput.tsx` 客户端组件 — 带 "从标题生成" 按钮，点击后调用 `suggestSlug(title)` 服务器 action
- `WorkForm` 中替换原有静态 slug 字段为 `SlugInput`

### 修改文件
- `actions.ts` — `workUpdateSchema` 新增 palette 字段，`updateWork` 解析 palette
- `[id]/page.tsx` — 新增 `PaletteEditor` 组件 + 导入 `SlugInput`
- `[id]/SlugInput.tsx` — 新建客户端组件

### 验证
- `npm run build` — 通过，29 pages generated

---

## Plan 2: Works & Media — P2 完成

### 媒体库搜索与筛选
- `admin/media` 页面新增搜索框（`ilike` 模糊匹配 original_name）和类型下拉（图片/视频/其他）
- URL query params (`?search=xxx&type=image`) 驱动服务端筛选
- 显示筛选结果计数 + 清除筛选链接

### 可视化媒体选择器
- 新建 `src/components/admin/MediaPicker.tsx` — 客户端组件
  - 缩略图网格展示（image 直接显示、video 预览、其他 file 占位）
  - single 模式（radio 行为）/ multi 模式（checkbox 行为）
  - 选中态蓝色边框高亮 + "已选"/"✓" 标签
  - 已选计数 + 一键清除
- 集成到作品编辑页：
  - 新增媒体块 → single 模式
  - 新增图库块 → multi 模式
  - 编辑媒体块 → single 模式（预选当前媒体）
  - 编辑图库块 → multi 模式（预选已有媒体）

### 提交记录
- `e86c61c` — P0+P1（媒体块编辑 + palette + slug）
- `a4becad` — P2（媒体库搜索 + 可视化选择器）
- 均已推送 GitHub，Vercel 自动部署中

---

## 修复 VideoBlockCard / BeforeAfterBlockCard 缺失

- 构建报错：page.tsx 引用了 VideoBlockCard 和 BeforeAfterBlockCard 但未定义
- 新增 VideoBlockCard 组件：复用 video 块编辑表单（media_id single picker + sort_order + is_visible + caption）
- 新增 BeforeAfterBlockCard 组件：before/after 双媒体选择器 + 双侧预览 + 编辑表单
- `npm run build` 通过，TypeScript 零类型错误

---

## PDF 块类型完成

- 新增 `pdfBlockSchema` + `createPdfBlock` + `updatePdfBlock` 到 actions.ts
- 新增 `PdfBlockCard` 组件到 page.tsx — PDF 媒体选择 + 编辑表单
- "新增 PDF 块"创建表单已集成到 BlockEditor
- 现在 6 种块类型全部可编辑：text / media / gallery / video / before_after / pdf
- `npm run build` 通过，已推送 `2e78502`

---

## 前台渲染：video / pdf / before_after CMS 块

### 媒体引用富化
- 所有单媒体 action 创建/更新时解析 `media_assets` 并存储 `media_ref`
- `createMediaBlock` / `updateMediaBlock` → `{ media_id, caption, media_ref }`
- `createVideoBlock` / `updateVideoBlock` → 同上
- `createPdfBlock` / `updatePdfBlock` → 同上
- `createBeforeAfterBlock` / `updateBeforeAfterBlock` → `{ before_media_id, after_media_id, caption, before_media_ref, after_media_ref }`

### 数据映射 (repository.ts)
- `toPublicBlocks` 新增 video / pdf / before_after 映射
- `toRefs` 辅助函数兼容 `media_ref` 单对象 / `media_refs` 数组

### 前台渲染 (works/[slug])
- Video 块：标签 + 链接按钮
- PDF 块：文件标签 + 查看链接（新窗口打开）
- 下箭头兼容所有块类型

### 类型扩展
- `ContentBlock` 新增 `video` 和 `pdf` 联合类型

### 验证
- `npm run build` 通过
- 已推送 `aff69e2`

---

## 精细化完善 Part 2

### before/after 图片渲染 + video 播放器 + SEO
- `ContentBlock` 类型新增 `beforeMedia`/`afterMedia` 可选字段
- `toPublicBlocks` 映射 `before_media_ref`/`after_media_ref` → `WorkMedia`
- `/works/[slug]` before/after 块从占位文字改为 `WorkMediaFrame` 实际渲染图片
- video 块从链接按钮改为 `<video controls>` 内嵌播放器
- PDF 块保持链接按钮（不适合内嵌）
- `generateMetadata()` 生成作品详情 OG/Twitter Card 标签
- 根 `layout.tsx` 增强 Twitter card + OG 默认图

### 区块拖拽排序
- 新建 `BlockListWrapper.tsx` 客户端组件，原生 HTML5 Drag & Drop
- 新增 `reorderWorkBlocks` server action，批量更新 `sort_order`
- 拖动透明度变化 + 目标位置氰色高光环
- 失败时回退到原始顺序

### Skeleton 加载状态
- 新建 `src/components/ui/Skeleton.tsx` — `SkeletonPulse`/`SkeletonLine`/`SkeletonCard`/`WorksListSkeleton`/`WorkDetailSkeleton`/`AdminTableSkeleton`/`MediaGridSkeleton`
- 5 个 loading.tsx：`/works`、`/works/[slug]`、`/admin/works`、`/admin/works/[id]`、`/admin/media`
- 无外部依赖，自建 `cx()` 轻量 className 拼接

### MediaPicker 搜索/筛选/排序增强
- 客户端搜索：文件名 + alt_text 模糊匹配
- 类型下拉：全部/图片/视频/其他
- 排序：最新优先/最早优先/名称 A-Z
- multi 模式新增"全选"/"取消"按钮
- 筛选结果计数 + 已选计数

### Admin 媒体库排序
- 新增 `sort` searchParam：newest/oldest/name/size
- 清除按钮在非默认排序时显示

### 提交
- `f77170f` — before/after图片+video+SEO+拖拽排序
- `ae910fd` — Skeleton + MediaPicker增强

---

## 全面补完 Part 3

### SEO 基础设施
- `public/robots.txt` — Disallow /admin 和 /api，指向 sitemap
- `src/app/sitemap.ts` — 动态生成，含静态页 + 已发布作品 slug

### 全局错误边界
- `src/app/error.tsx` — 捕获未处理异常，显示铜色图标、中文提示、重新加载按钮

### Admin 仪表盘增强
- 5 个统计卡片：已发布/草稿/未读消息/媒体文件/分类标签
- 快捷操作按钮：新建作品/上传媒体/查看消息/网站设置
- 最近更新作品列表（status 徽章 + 编辑链接）
- 最近消息预览（未读小圆点 + 相对时间）
- 修复：`is_read` → `status: new` 字段对齐数据库 schema

### 简历 CMS 同步
- 前台 `/resume` 页面从同步组件改为异步
- `getResumeData()` 从 Supabase `resumes` 表读取基本字段（name/email/phone/strengths 等）
- 复杂数据（experience/campus/education/expertise）保持代码静态维护
- CMS 不可用时静默回退到静态数据

### 联系表单客户端验证
- ContactForm 新增 `FieldError` 状态 + `validate()` 函数
- 姓名（≥2 字符）、邮箱（正则）、消息（≥10 字符）
- 红色边框 + AlertCircle 图标行内错误提示
- 输入时自动清除对应字段错误
- 支持服务端返回 `fieldErrors` 映射渲染
- 成功状态绿色 CheckCircle 图标

### 提交
- `0806c0d` — SEO基础+error边界+仪表盘+简历同步+表单验证

---

## 2026-06-23 (续)

### 简历页 UI 微调
- ContactForm 输入框边框从 `border-white/10` 降为 `border-white/[0.06]`，背景从 `bg-black/25` 改为 `bg-white/[0.02]`，与卡片边框视觉一致
- 简历页 hero 区域手机端放大：`text-5xl` → `text-[2.85rem] sm:text-6xl`，增加垂直 padding 和最小高度
- 已推送 `228dd10`

### 简历联系卡片手机端优化
- InlineField 标签字号 `text-xs` → `text-[13px]`，宽度 `2.6rem` → `2.8rem`，颜色 `text-white/50` → `text-white/55`
- 表单手机端输入框收窄：`max-w-[97%] mx-auto`（sm 以上恢复全宽 grid）
- 字段顺序调整：岗位名称/薪资范围移到岗位描述/项目需求**之前**
  - 新顺序：姓名 → 邮箱 → 公司 → 岗位名称 → 薪资范围 → 岗位描述 → 备注
- 已推送 `2a5f3ba`
