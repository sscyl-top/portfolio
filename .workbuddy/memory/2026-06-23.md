# 2026-06-23 工作日志

## 全局文字管理功能修复
- 问题：后台「全局文字」入口存在（/admin/settings/text-content），但数据库 text_content 表未在 Supabase 中实际创建
- 修复：提供 SQL 脚本，用户在 Supabase SQL Editor 手动执行
- 遇到的问题：PostgreSQL 不支持 `CREATE POLICY IF NOT EXISTS` 语法（错误 42601），改用 `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` 异常捕获写法解决
- 最终状态：SQL 成功执行，text_content 表 + 21 条预设数据已就位
