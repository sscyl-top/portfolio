# 2026-06-23

## 精细化 — 输入框边框颜色
- ContactForm.tsx: 表单输入框 border 从无色（默认白）改为 `border-white/10`，与联系卡片边框统一
- 新增 4 个 loading.tsx 骨架屏：works/[slug]、admin/works、admin/works/[id]、admin/media
- 新建 Skeleton.tsx 组件库（WorksListSkeleton / WorkDetailSkeleton / AdminTableSkeleton / MediaGridSkeleton）

## 简历页手机端压缩
- 教育背景成就卡片：h-20→min-h-[60px]，GPA字号手机端text-2xl，detail间距mt-0.5
- 联系卡片：标签+输入框同行（grid-cols-[3rem_1fr]），p-4→p-3.5，gap-4→gap-3
- 输入框高度 min-h-10→min-h-9、px-3.5→px-3 py-1.5；标题 text-3xl→text-2xl（手机）
- textarea 高度压缩（min-h-20→16, min-h-16→12）；按钮 min-h-11→9
- 用户要求：改完自动推送（不再需要单独确认 push）

## 邮箱统一
- 将 `src/data/portfolio.ts` 社交链接和简历联系邮箱从 `3624457672@qq.com` 改为 `hello@sscyl.top`
- 将 `src/app/api/contact/route.ts` 通知收件人从 `3624457672@qq.com` 改为 `hello@sscyl.top`
- `src/lib/admin.ts` 的 `defaultAdminEmail` 保持不变（Supabase 鉴权用）

## 待办
- Task #20：Admin 图片选择器搜索/筛选增强
