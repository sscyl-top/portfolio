# 2026-06-23 工作日志

## 全局文字管理功能修复（已完成，已部署）
- 问题：后台「全局文字」入口存在但 text_content 表未在 Supabase 中创建
- 修复过程：
  1. 第 1 次 SQL：PostgreSQL 不支持 `CREATE POLICY IF NOT EXISTS`（错误 42601）
  2. 第 2 次 SQL：改用 DO $$ EXCEPTION 异常捕获写法，成功
  3. 但线上仍报错 `Could not find table 'public.text_content'` — 用户可能执行到了错误的 Supabase 项目
  4. 第 3 次 SQL：DROP + CREATE 完整重建表（含 RLS 权限与 CMS 表一致）+ INSERT 数据 → 成功
- 最终：SQL 执行成功，代码已 git push (aaa008f)，Vercel 自动部署中
- 教训：以后涉及数据库变更，必须确认用户执行到了正确的 Supabase 项目；SQL 应一次给对完整版本
# 2026-06-23 工作日志

## VisualBlockEditor 后台作品编辑体验改造

### 完成内容
- 新建 `src/components/admin/VisualBlockEditor.tsx` 替换旧 BlockEditor
- 新建 `src/lib/cms/upload-media.ts` 客户端媒体上传工具
- `src/app/admin/(protected)/works/actions.ts` 追加 `createBlockDirect`、`deleteBlockDirect`、`updateBlockDirect` Server Actions
- `src/app/admin/(protected)/works/[id]/page.tsx` 导入并渲染 VisualBlockEditor

### VisualBlockEditor 功能
1. **可视化块列表** — 卡片式垂直堆叠，显示类型图标和预览
2. **拖拽排序** — HTML5 Drag and Drop，拖拽手柄调整顺序
3. **文件拖拽上传** — 拖拽文件到任意位置插入，自动识别类型创建块
4. **多文件批量上传** — 支持一次选择多个文件
5. **内联文本编辑** — 点击编辑按钮，直接在页面上编辑标题和正文（防抖保存）
6. **块间插入按钮** — 每个块之间有 "+" 触发按钮，点击弹出添加菜单
7. **添加内容块菜单** — 选择块类型，文本直接创建，媒体类型触发文件选择
8. **删除块** — 每个块有删除按钮

### 待完善
- 媒体/视频/PDF 块的 inline 编辑（更换媒体、编辑说明文字）
- before_after / gallery 类型的完整创建流程
- 图片裁剪功能

## text_content 生产环境修复（15:26）
- 线上 `https://sscyl.top/admin/settings/text-content` 报错：`Could not find table 'public.text_content' in schema cache`
- 根因：text_content 迁移从未在生产 Supabase (hnujowombcgfxledpnxe) 上执行，deploy.sql 未包含此表
- 创建 `supabase/migrations/20260624000000_fix_text_content_production.sql`（含建表+RLS+种子数据）
- 更新 `deploy.sql` 追加 text_content 部分
- 待用户到 Supabase Dashboard SQL Editor 手动执行

## 第二轮完善（下午）

### 已完成
1. **媒体/视频/PDF 块内联编辑** — InlineMediaEditor 组件
   - 显示媒体预览（图片/video/PDF）
   - 说明文字编辑（防抖保存）
   - "更换媒体"按钮（上传替换）
   - "裁剪"按钮（仅图片类型）

2. **图库块创建流程** — 菜单选择"图库"后触发多选文件输入，上传后自动创建 gallery 块

3. **Before/After 块创建流程** — 菜单选择"对比"后创建空块并自动进入编辑模式，依次选择 before/after 图片

4. **图片裁剪功能** — ImageCropper 组件（react-image-crop）
   - 裁剪模态框，支持自由裁剪或固定比例（1:1, 4:3, 16:9）
   - 裁剪后上传为新文件并替换块的媒体
   - 裁剪后的 Blob 通过 uploadMediaBlob 上传到 Supabase

### 新增文件
- `src/components/admin/ImageCropper.tsx` — 图片裁剪模态框组件

### 修改文件
- `src/components/admin/VisualBlockEditor.tsx` — 大幅扩展，接入所有 inline 编辑器
- `src/lib/cms/upload-media.ts` — 新增 uploadMediaBlob 函数
- `src/app/admin/(protected)/works/actions.ts` — 新增 updateBlockMediaRef Server Action

### 构建状态
✅ 构建通过

## text_content 最终验证与修复（19:04）

### 诊断
- 通过 REST API 直连 `hnujowombcgfxledpnxe.supabase.co` 验证：
  - text_content 表已存在，21 条种子数据完整
  - anon key 和 service_role key 均可正常读取
- 线上 admin 页面报错原因为 Vercel 缓存了旧错误页面（Next.js ISR/CDN 缓存）

### 修复
- `src/app/admin/(protected)/settings/text-content/page.tsx` 添加 `export const dynamic = "force-dynamic"` 防止缓存
- Git push (bcf75f4) 触发 Vercel 自动重新部署
- 部署完成后 admin 页面应正常显示 21 条文字记录

## B2 布局系统实现（20:00）

### 用户需求
- 要类似 Behance 的布局体验（B2方向）：纵向流式 + 可调整列宽/布局
- 不要真正的自由画布（B1），要适合作品集阅读体验

### 完成改动

1. **`src/components/admin/VisualBlockEditor.tsx`**
   - 添加 `LayoutBar` 组件：编辑块时显示布局控制条
   - 宽度选择：通栏(full) / 约束(contained, 默认) / 窄版(narrow)
   - 文本块：额外显示对齐选择（左对齐/居中）
   - 图库块：额外显示列数选择（2/3/4列）
   - `getLayout()` / `withLayout()` 辅助函数

2. **`src/data/portfolio.ts`**
   - 新增 `BlockLayout` 类型：`{ width?, align?, columns? }`
   - `ContentBlock` 所有 variant 加 `layout?: BlockLayout`

3. **`src/lib/cms/repository.ts`**
   - `toPublicBlocks()` 读取 `payload.layout` 并传给前台

4. **`src/app/works/[slug]/page.tsx`**
   - 新增 `layoutWidthClass()` / `layoutSectionClass()` 辅助函数
   - 页面结构重构：头部信息约束在 `max-w-6xl` 内，blocks 区域独立
   - 每个块根据 `layout` 应用宽度/对齐/列数

### 构建状态
✅ `next build` 通过，零错误

### 待确认
- "通栏"满宽效果是否需要真正 edge-to-edge（需额外 CSS 技巧）
- 后台 `LayoutBar` 样式是否需要微调

## text_content 问题排查与加固（20:20）
- 线上 text_content API 验证：表存在、21 条数据可正常读取（publishable key）
- page.tsx 加固：Supabase 查询外包 try-catch 防止未捕获异常导致 Next.js 错误页
- fix_text_content_production.sql 改为独立版：内联创建 private schema + is_admin/set_updated_at 函数，不依赖其他迁移
- 提交 969caa5 并推送，触发 Vercel 自动部署

## HANDOFF_FINAL.md 交接文档生成（23:59）
- 创建完整交接文档 `HANDOFF_FINAL.md`，覆盖 9 个章节
- 核心内容：text_content 表缺失修复方案（含完整可执行 SQL）、潜在 Auth 弯引号问题、邮箱不一致排查、媒体 nil UUID、DynamicText 未集成到前台
- 关键文件速查表：按功能模块分类列出所有文件路径和用途
- 确认了前台目前不依赖 text_content 表（DynamicText 组件未被任何页面引用），前端文字全部来自静态 portfolio.ts
