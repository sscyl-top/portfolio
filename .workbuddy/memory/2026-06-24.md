# 2026-06-24 工作日志

## 修复：首页多余「专业能力」标题
- `CapabilityBands.tsx` 中 `strengthsTitle` 条件渲染块（297-303行）在 Hero 和 5年+经验面板之间插入额外标题区域
- 删除该块，首屏直接进入经验面板内容
- commit: `215aca6`，已推送到 Vercel

## 邮箱更换：3624457672@qq.com → hello@sscyl.top（仅首页终场）
- **保留改动**：`portfolio.ts` 静态数据源（首页终场 CapabilityBands + 简历页 ContactFinale 读取此邮箱）→ `hello@sscyl.top`
- **已恢复原样**（过度修改）：`admin.ts` 管理员默认邮箱、所有测试文件 → 恢复为 `3624457672@qq.com`
- 教训：用户说改终场就只改终场，不要连带改管理后台/测试
- commits: `fd20ec5`（全量）→ `f8068dc`（回退多余修改）

## 简历页 ContactFinale 手机端优化
- 仅手机端压缩间距和字号（`md:` 前缀 class），桌面端样式完全不变
- commit: `27e42b0`

## fix: 作品详情媒体展示扩大 + 版本归档节流

### 媒体展示区域扩大 ~10%（`WorkContentBlocks.tsx`）
- 单张 media：从 `aspect-video`/`aspect-[16/9]` 改为 `min-h-[65vh]`，不再受宽高比约束
- gallery 图片：从 `aspect-[4/3]` → `aspect-[3/2]`（更高）
- video：从 `aspect-video` → `min-h-[65vh]`
- free layout：从 `h-[520px] md:h-[720px]` → `h-[580px] md:h-[800px]`

### 自动版本归档节流（`versions.ts`）
- 问题：每次操作（创建/更新/删除块、排序等）都调用 `autoArchiveAfterChange`，点几下就产生 100+ 版本
- 修复：在 `archiveWorkVersion` 中对 source="auto" 加入 3 分钟节流窗口
- 查询最近一次 auto 版本的 created_at，间隔 < 3min 则静默跳过
- 手动归档（source="manual"）不受影响
- commit: `3deeca1`
