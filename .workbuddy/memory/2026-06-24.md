# 2026-06-24 工作日志

## 布局约束操作
- 备份了所有布局约束到 `backup-layout-constraints.md`
- 第一次移除约束后恢复（用户反馈太奇怪）
- 第二次移除约束：layoutWidthClass max-w、自由排版高度、媒体 min-h、Before/After aspect-ratio
- **修复**：WorkMediaFrame 的 object-fit 改动误伤封面/Hero；新增 `fit` prop (cover/contain)，仅内容块传 `fit="contain"`，封面保持 `object-cover`
- 最终恢复全部约束，宽度调整为 max-w-[1420px]（与导航栏对齐），移除 fit prop
- 提交记录：df78320(恢复) → 1def5f8(移除) → a175033(修复封面) → b24c7f1(恢复全部+对齐导航栏)

## 布局按钮跳动修复
- VisualBlockEditor 中 getLayout() 每次返回新对象引用导致 LayoutBar 反复重渲染
- 修复：useMemo 稳定 layout 对象 + 组件卸载清除防抖定时器

## 图库1列 + Lightbox
- 后台 GALLERY_COLUMN_OPTIONS 增加「1列」选项
- columns 类型扩展为 1|2|3|4（VisualBlockEditor + portfolio.ts + WorkContentBlocks）
- 新增 GalleryLightbox 组件：全屏遮罩、左右箭头切换、ESC/点击背景关闭、键盘方向键导航、图片计数器
- 图库图片 cursor-zoom-in + 点击打开 Lightbox

## 保存成功 Toast
- 新增 Toast + ToastHandler 组件：右上角浮窗、3秒自动关闭、成功图标
- updateWork/updateWorkMedia/updateWorkTaxonomy 改为 redirect 带回 toast 参数

## 标题区域对齐 + 文本右对齐
- 作品详情 header 从 max-w-5xl 改为 max-w-[1420px]（与导航栏/内容块对齐）
- WorkReactions 互动区同步改为 max-w-[1420px]
- BlockLayout.align 扩展为 left/center/right，LayoutBar 增加右对齐按钮
- TextBlock 渲染支持 text-right
- 修复 ToastHandler 残留的未使用 useRouter 引用

## 后台排版大改造（22:00）
- **导航栏遮挡修复**：AdminShell 内容区增加 pt-20 md:pt-28，避免被固定 SiteHeader (h-24) 遮挡删除按钮等
- **左右分栏布局**（参考站酷）：lg 以上屏幕左列主表单+编辑器+版本历史，右列 320px 放辅助选项（私密预览/媒体/分类标签）
- **右侧表单紧凑化**：PrivatePreview/Media/Taxonomy 全部缩小字号、间距、去掉冗余描述
- **新增 CollapsibleSection 通用折叠组件**：箭头动画 + 标题 + 可选描述 + 操作按钮插槽
- **TaxonomyForm 默认折叠**：显示已选项计数，点击展开分类/标签选择
- **VersionHistoryPanel 默认折叠**：显示版本数量，点击展开版本列表和操作
- VersionHistoryPanel 内部拆分出 VersionList 子组件，降低主组件复杂度
